<%= form_with(model: created_field, local: true) do |form| %>
  <% if created_field.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(created_field.errors.count, "error") %> prohibited this created_field from being saved:</h2>

      <ul>
      <% created_field.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content %>
  </div>

  <div class="field">
    <%= form.label :document_id %>
    <%= form.number_field :document_id %>
  </div>

  <div class="field">
    <%= form.label :field_id %>
    <%= form.number_field :field_id %>
  </div>

  <div class="field">
    <%= form.label :admin_user %>
    <%= form.number_field :admin_user %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
