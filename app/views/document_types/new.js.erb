jQuery(document).ready(function($){
	$('<%= j render "form", document_type: @document_type %>').on('hidden.bs.modal', function (e) {
	$(this).remove();
	$('.modal-backdrop').remove();
	}).modal();

});