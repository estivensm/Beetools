jQuery(document).ready(function($){
$('#product-modal').modal('hide')
toastr["success"]("El Campo fue creado con <b> Exito!</b>");


            var markup = "<tr><td>" + "<%= @field.field_type %>" + "</td><td>" + "<%= @field.name %>" + "</td><td>" + '<%= link_to "Edit", edit_field_path(@field), remote:true %>' + '<%= link_to "Destroy", @field, method: :delete, data: { confirm: "Are you sure?" }, remote:true %>' + "</td>";
            $(".table_form tbody").append(markup);

        
        // Find and remove selected table rows
        //$(".delete-service").click(function(){
            //$(".table_services tbody").find('input[name="record"]').each(function(){
            	//if($(this).is(":checked")){
                    //$(this).parents("tr").remove();
                //}
            //});
        //});   
});